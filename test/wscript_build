#! /usr/bin/env python
# encoding: utf-8

bld.program(
    features = 'cxx',
    source   = ['sak_tests.cpp'] + bld.path.ant_glob('src/*.cpp'),
    target   = 'sak_tests',
    use      = ['sak_includes', 'gtest', 'boost_includes'])




